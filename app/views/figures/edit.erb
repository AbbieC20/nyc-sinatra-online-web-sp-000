<!DOCTYPE html>
<html>
  <head>
    <title>Figures</title>
  </head>

  <body>
    <h2> Edit the Figure </h2>
    <h3> Edits for <%= @figure.name %></h3>

    <form action="/figures/<%= @figures.id %>" method="POST">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <label>Edit the Figure's Name: </label><input id="Artist Name" type="text" name="artist[name]" value="<%= @song.artist.name %>">
      <br><br>
      <br><input id= "Save" type="submit" value="Edit Figure">
      </form>
  </body>
</html>
